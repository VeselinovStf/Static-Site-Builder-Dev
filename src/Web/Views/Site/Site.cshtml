@model SiteRenderingViewModel
@{

    Layout = "~/Views/Shared/_SiteAdminLayout.cshtml";
    //ViewData["ProjectId"] = Model.ProjectId;
}
@foreach (var item in Model.Widgets)
{
    <hr />
    <h1>@item.Name</h1>
}
<div class="embed-responsive embed-responsive-16by9">
    <iframe class="embed-responsive-item" @*src="@Model.PresentationLink" *@ allowfullscreen></iframe>
</div>